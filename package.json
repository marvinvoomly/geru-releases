{
  "name": "GERU",
  "description": "This is GERU Desktop Application",
  "author": "Etison LLC DBA GERU",
  "version": "0.1.4",
  "private": true,
  "homepage": "./",
  "main": "main.js",
  "devDependencies": {
    "electron": "^14.0.0",
    "electron-builder": "^20.44.4"
  },
  "dependencies": {
    "electron-is-dev": "^1.2.0",
    "electron-updater": "^5.2.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marvz73/geru-releases.git"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
  
  
}
